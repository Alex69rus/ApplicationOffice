@using ApplicationOffice.Web.UI.Approvals.Enums
@if (Status == ApplicationStatus.New)
{
    <MatChip Style="background-color: skyblue" Label="Новая"></MatChip>
}
else if (Status == ApplicationStatus.Approved)
{
    <MatChip Style="background-color: lightgreen" Label="Согласовано"></MatChip>
}
else if (Status == ApplicationStatus.Rejected)
{
    <MatChip Style="background-color: rgb(238,43,43)" Label="Отклонено"></MatChip>
}

@code {

    [Parameter]
    public ApplicationStatus Status { get; set; }

}
